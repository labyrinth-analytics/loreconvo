# LoreConvo v0.5.0 - Pinned direct dependencies
# Exact versions required for Anthropic marketplace compliance
mcp==1.28.1
click==8.3.3
anthropic==0.87.0
packaging==25.0
# Semantic search (hybrid vector + BM25). Base deps since 2026-07-25
# (spec amendment A1): fastembed/onnxruntime replace sentence-transformers,
# so there is one install path for every tier and Pro is a license flag.
lancedb==0.30.2
pyarrow==24.0.0
fastembed==0.8.0
# fastembed 0.8.0 caps onnxruntime <1.24 on Python <=3.13 but requires >=1.24.2
# on 3.14, so no single pin covers requires-python >=3.10.
onnxruntime==1.23.2; python_version < '3.14'
onnxruntime==1.28.0; python_version >= '3.14'
