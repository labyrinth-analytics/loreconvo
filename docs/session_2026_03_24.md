# Session Summary — 2026-03-24
**Project:** Project Ron / Side Hustle
**Surface:** Cowork
**Topics:** Marketplace publishing research, installation blockers, repo strategy, SDLC, GitHub setup

---

## Key Decisions

### 1. "knowledge-work-plugins" is RESERVED by Anthropic
The target marketplace name was not a third-party submission target -- it is Anthropic-internal.
Distribution strategy corrected to:
  - Phase 1: Self-hosted GitHub marketplace at labyrinth-analytics/claude-plugins
  - Phase 2: Official submission via https://clau.de/plugin-directory-submission

### 2. Installation architecture: uvx (unanimous choice)
Both ConvoVault and ProjectVault now use identical install pattern:
  { "command": "uvx", "args": ["convovault"] }
  { "command": "uvx", "args": ["projectvault"] }
uvx = npx for Python. Downloads and runs from PyPI in isolated env. No hardcoded paths.
Requires: uv installed once (curl -LsSf https://astral.sh/uv/install.sh | sh)

### 3. Three-repo public strategy
  labyrinth-analytics/convovault    -- public, plugin files + Python package
  labyrinth-analytics/projectvault  -- public, plugin files + Python package
  labyrinth-analytics/claude-plugins -- public, marketplace catalog (marketplace.json)
  labyrinth-analytics/side-hustle   -- PRIVATE, stays private, all dev happens here

### 4. GitHub authentication: gh CLI with HTTPS
SSH keys not configured on Debbie's Mac. Using HTTPS + gh auth login.
Both accounts configured in keyring:
  - labyrinth-analytics (active)
  - debbie-shapiro (inactive)
Git pushes now work from the Mac terminal.

### 5. Website left blank in GitHub About panels
Labyrinth Analytics website exists only locally. Will add URL once site is live.

---

## Work Completed This Session

### Files created/modified in private monorepo (~/projects/side_hustle):
- ron_skills/convovault/pyproject.toml -- NEW: PyPI package definition
- ron_skills/convovault/src/server.py -- MODIFIED: added main() entry point
- ron_skills/convovault-plugin/.mcp.json -- FIXED: now uses uvx (was hardcoded paths)
- ron_skills/projectvault-plugin/.mcp.json -- FIXED: now uses uvx (was python3 -m)
- ron_skills/convovault-plugin/README.md -- REWRITTEN: public-facing, Mermaid diagram
- ron_skills/projectvault-plugin/README.md -- REWRITTEN: public-facing, Mermaid diagram
- docs/SDLC.md -- NEW: full repo strategy, GitHub setup checklist, marketplace.json template
- docs/PUBLISHING.md (both products) -- NEW: publishing research and submission guide
- scripts/release.sh -- NEW: release automation script

### Pushed to public GitHub repos:
- https://github.com/labyrinth-analytics/convovault -- live with plugin files + README
- https://github.com/labyrinth-analytics/projectvault -- live with plugin files + README
- https://github.com/labyrinth-analytics/claude-plugins -- exists, README stub only

---

## Open Questions / Next Steps

1. PyPI accounts: need to create pypi.org account and publish both packages
   before uvx convovault / uvx projectvault will resolve for end users

2. marketplace.json: needs to be added to labyrinth-analytics/claude-plugins
   once both products have tagged releases (v0.3.0, v0.1.0)

3. Git tags: neither public repo has a release tag yet -- needed for marketplace pinning
   Commands (run from Mac after committing the private monorepo):
     cd /tmp/convovault-public && git tag v0.3.0 && git push origin v0.3.0

4. Private monorepo commit: all session work is staged but NOT committed
   (Cowork VM git locks prevent committing from this session)
   Commands to run from Mac terminal:
     cd ~/projects/side_hustle
     rm -f .git/index.lock .git/HEAD.lock
     git status
     git commit -m "Fix MCP install blockers + add pyproject.toml, READMEs, SDLC, release script"
     git push origin master

5. GitHub About panels: fill in description + topics on all three public repos
   (Website field left blank until labyrinth-analytics site goes live)

6. Website: Labyrinth Analytics site exists locally only -- needs to be deployed
   before adding website URL to GitHub repo About panels

---

## Artifacts Created

- docs/SDLC.md -- repo strategy and release workflow documentation
- docs/session_2026_03_24.md -- this file
- ron_skills/convovault/docs/PUBLISHING.md -- ConvoVault publishing guide
- ron_skills/projectvault/docs/PUBLISHING.md -- ProjectVault publishing guide
- scripts/release.sh -- release automation script

---

## Technical Notes

- ConvoVault pyproject.toml uses flat package mapping: "convovault" = "src"
  because src/ IS the package root (src/server.py, src/core/, etc.)
- ProjectVault already had pyproject.toml and main() -- no changes needed
- Both products' plugin.json files are correct and kebab-case compliant
- release.sh uses SSH git URLs -- update to HTTPS before running
  (change git@github.com:labyrinth-analytics/ to https://github.com/labyrinth-analytics/)
